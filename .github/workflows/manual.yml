name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: Hello
        required: true
        description: A cheerflu word
jobs:
  run:
   runs-on: ubuntu-latest
   steps:
     - run: echo "${{ inputs.greeting }}"
